import BottomSheet from "../BottomSheeet/BottomSheet";

type Props = {};

const CustomBottomSheet = (props: Props) => {
  return <BottomSheet open={true}></BottomSheet>;
};

export default CustomBottomSheet;
